# Testing Git and github
import pandas as pd
import numpy as np

df = pd.read_excel("Location_Individual_file.xlsx")
print(df)
print(not df)
a = 2 + 3
b = pd.to_date(a)


def NewFunction():
    # Changing things in the master branch to merge to the the other branch
    # This is the new function in the master branch
    pass
